# SPDX-FileCopyrightText: 2023 Comcast Cable Communications Management, LLC
# SPDX-License-Identifier: Apache-2.0
---
name: CI

on:
  push:
    branches:
      - main
    paths-ignore:
      - README.md
      - CONTRIBUTING.md
      - MAINTAINERS.md
      - LICENSE
      - NOTICE
    tags:
      - 'v[0-9]+.[0-9]+.[0-9]+'
  pull_request:
  workflow_dispatch:

permissions:
  pull-requests: read
  contents:      write
  packages:      write

jobs:
  ci:
    uses: xmidt-org/shared-go/.github/workflows/ci.yml@041a1989ad6bc757d073c6a643112353e7506079 # v4.9.23
    with:
      release-type:   library
      yaml-lint-skip: false
    secrets: inherit
